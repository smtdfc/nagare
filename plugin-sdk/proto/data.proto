syntax = "proto3";
package data;

option go_package = "github.com/smtdfc/nagare/plugin-sdk/proto";

message Empty {}

message PluginMetadata {
    string id = 1;
    string name = 2;
}

service Plugin {
  rpc Metadata (Empty) returns (PluginMetadata);
}