info:
  name: install-local
  type: http
  seq: 2

http:
  method: POST
  url: "{{NAGARE_REST_API}}/user/plugins/install-local"
  body:
    type: json
    data: |-
      {
        "path":"/home/smtdfc/Projects/nagare/plugin/built_ins/example/nagare.plugins.example.nagare_plugin"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
