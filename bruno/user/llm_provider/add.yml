info:
  name: add
  type: http
  seq: 2

http:
  method: POST
  url: "{{NAGARE_REST_API}}/user/llm-providers/add"
  body:
    type: json
    data: |-
      {
        "name":"Groq",
        "compatible":"OpenAI",
        "apiKey":"{{GROG_API_KEY}}",
        "models":[
          "openai/gpt-oss-20b"
        ],
        "baseUrl":"https://api.groq.com/openai/v1"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
