info:
  name: list
  type: http
  seq: 1

http:
  method: GET
  url: "{{NAGARE_REST_API}}/user/llm-providers/list"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
