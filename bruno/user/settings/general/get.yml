info:
  name: get
  type: http
  seq: 2

http:
  method: GET
  url: "{{NAGARE_REST_API}}/user/settings/general/get"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
