info:
  name: create
  type: http
  seq: 1

http:
  method: POST
  url: "{{NAGARE_REST_API}}/user/chat/session/create"
  body:
    type: json
    data: |-
      {
        "title":"Hello"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
